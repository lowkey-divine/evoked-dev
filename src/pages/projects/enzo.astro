---
import ElevationLayout from '../../layouts/ElevationLayout.astro';
import Hero from '../../components/elevation/Hero.astro';
import Section from '../../components/elevation/Section.astro';
---

<ElevationLayout
  title="Enzo"
  description="A game is forming."
>
  <Hero
    eyebrowParts={["Projects", "Forming"]}
    headline="Enzo."
    metaphor={["A game is forming."]}
  />

  <Section marker="NOTA · BENE" closing={true}>
    <p class="enzo-patient">More soon.</p>
  </Section>
</ElevationLayout>

<style>
  .enzo-patient {
    font-family: var(--el-body);
    font-style: italic;
    font-size: clamp(1.15rem, 1.05rem + 0.5vw, 1.4rem);
    line-height: 1.5;
    color: var(--el-text-quiet);
    margin: 0;
    text-align: center;
  }
</style>
